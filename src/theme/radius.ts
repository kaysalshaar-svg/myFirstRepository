// ─── Serene Path — Design Token: Border Radius ─────────────────────────────

export const radius = {
  sm:   '0.25rem',   // 4px  — subtle
  md:   '0.5rem',    // 8px  — DEFAULT cards inner
  lg:   '0.75rem',   // 12px — buttons, inputs
  xl:   '1rem',      // 16px — cards
  '2xl':'1.5rem',    // 24px — large cards, modals
  '3xl':'2rem',      // 32px — hero banners
  full: '9999px',    // pill — chips, badges, nav items
}
